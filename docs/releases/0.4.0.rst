.. _0_4_0:

RP 0.4.0
=========

HolyValue
---------

* [x] use default dataset to avoid initialization

  .. code-block:: python

    configos.holyvalue.DATABASE = configos.holyvalue.DataBase(
        __file__,
        current=configos.holyvalue.DataSet(),
    )

* [x] make code less verbose, covert;

  .. code-block:: python

    MAX_FOOTERHEADER_AREA_COUNT = configos.HV(
        default=5,
        datatype=configos.DataType.INT_PLUS,
    )

  .. code-block:: python

    MAX_FOOTERHEADER_AREA_COUNT = configos.HV_INT_PLUS(default=5)

* [x] think about evaluating the limits on runtime, if not pytest for example
  will not start, or returnvalue of executables will be ?ambigous?.

Fix
---

* [x] fix check of percent convertion. Current=15; Limit=10

  .. code-block:: python

    HEADER_MAX_SIZE = configos.HV(
        default=15,
        limit=10,
        datatype=configos.DataType.PERCENT_PLUS,
    )

* [x] fix to detect auto generated name

  .. code-block:: python

    configos.HV(
        default=15,
        limit=120,
        datatype=configos.DataType.PERCENT_PLUS,
    )
