# PR 0.4.0

## HolyValue

* [x] use default dataset to avoid initialization

    ```[python]
        configo.holyvalue.DATABASE = configo.holyvalue.DataBase(
            __file__,
            current=configo.holyvalue.DataSet(),
        )
    ```

* [x] make code less verbose, covert;

    ```[python]
    MAX_FOOTERHEADER_AREA_COUNT = configo.HV(
        default=5,
        datatype=configo.DataType.INT_PLUS,
    )
    ```

    ```[python]
    MAX_FOOTERHEADER_AREA_COUNT = configo.HV_INT_PLUS(default=5)
    ```

* [ ] think about evaluating the limits on runtime, if not pytest for example
will not start, or returnvalue of executables will be ?ambigous?.

## FIX

* [x] fix check of percent convertion. Current=15; Limit=10

    ```[python]
    HEADER_MAX_SIZE = configo.HV(
        default=15,
        limit=10,
        datatype=configo.DataType.PERCENT_PLUS,
    )
    ```

* [ ] fix to detect auto generated name

    ```[python]
    configo.HV(
        default=15,
        limit=120,
        datatype=configo.DataType.PERCENT_PLUS,
    )
    ```
